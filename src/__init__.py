"""
Initialization file for the `src` package.

This file is required by Python to treat the `src` directory as a package,
allowing modules within it (like pdf_extractor, model_inference, vocabulary, etc.)
to be imported from other parts of the project using dot notation (e.g., `from src.pdf_extractor import ...`).
"""

# Empty init file for src package
